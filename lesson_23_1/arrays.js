const arr = [1, 2, 3];

// console.log(arr[3]);
// console.log(arr.length);

// arr.push(5); добавить в конец
// arr.shift(); удалить с начала
// arr.pop(); удалить с конца
// filter
// const filtered = arr.filter((item) => item === 1)
// map
// const mapped = arr.map((item) => {
//   return item - 1;
// })
// forEach
// arr.forEach((item, index, array) => {
//   array[index] = item + 1;
// });
// reduce
// console.log(arr.reduce((acc, item) => {
//   return acc + item.salary < 15000 ? 0 : item.salary;
// }, 0))